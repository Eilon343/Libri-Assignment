# Base URL of the backend API (local development only).
# Leave unset to use the default in src/api.js: http://localhost:3000
#
# In Docker this is set to "" at build time so the browser calls the
# same origin ("/api/..."), which nginx proxies to the backend container.
VITE_API_URL=http://localhost:3000
